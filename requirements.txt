# Versions the optimized code was regression-tested with (tools/compare_runs.py).
# Scores depend on the numerical libraries (sklearn KMeans in particular), so keep
# baseline and candidate runs on the same versions when comparing outputs.
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
matplotlib==3.9.4
pysam==0.24.1
pillow==11.3.0
psutil
